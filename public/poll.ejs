Welcome !

<script src="/socket.io/socket.io.js"></script>
<script>
    var socket = io('/<%= poll.id %>');

    // reception d'une question
    socket.on('question.new', question => {
        /**
         * question.question
         * question.answers
         */
    });

    /**
     * répondre à une question
     * socket.emit('question.answer', {
     *      question: question.id,
     *      answer: 'My answer'
     * });
     */
</script>